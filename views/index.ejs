<%- include('partials/header', { pageTitle: 'Home' }) %>
<%- include('partials/navbar', { active: 'home' }) %>

<main class="main-content">
  <!-- Hero -->
  <section class="hero">
    <div class="hero-bg" style="background-image: url('<%= heroMovie.backdrop %>')"></div>
    <div class="hero-content">
      <div class="hero-badge">
        <% if (heroMovie.premium) { %>⭐ PREMIUM<% } else { %>🔥 TRENDING<% } %>
      </div>
      <h1 class="hero-title"><%= heroMovie.title %></h1>
      <div class="hero-meta">
        <span class="rating">★ <%= heroMovie.rating %></span>
        <span><%= heroMovie.year %></span>
        <span><%= heroMovie.duration %></span>
        <span><%= heroMovie.genre %></span>
      </div>
      <p class="hero-desc"><%= heroMovie.description %></p>
      <div class="hero-actions">
        <a href="/watch/movie/<%= heroMovie.id %>" class="btn btn-primary btn-lg">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
          Watch Now
        </a>
        <a href="/movies/<%= heroMovie.id %>" class="btn btn-icon-lg" title="More Info">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
        </a>
        <button class="btn btn-icon-lg" title="Add to Watchlist" onclick="addToWatchlist(<%= heroMovie.id %>)">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"/></svg>
        </button>
      </div>
    </div>
  </section>

  <!-- Continue Watching -->
  <% if (user) { %>
  <section class="section">
    <div class="section-header">
      <h2 class="section-title">Continue <span class="accent">Watching</span></h2>
      <a href="/movies" class="section-link">View All →</a>
    </div>
    <div class="content-row">
      <% continueWatching.forEach(item => { %>
      <a href="/watch/movie/<%= item.id %>" class="continue-card">
        <div class="thumb">
          <img src="<%= item.poster %>" alt="<%= item.title %>" loading="lazy">
          <div class="progress-bar"><div class="fill" style="width: <%= item.progress %>%"></div></div>
        </div>
        <div class="info">
          <h4><%= item.title %></h4>
          <p><%= item.genre %> • <%= item.duration %></p>
        </div>
      </a>
      <% }) %>
    </div>
  </section>
  <% } %>

  <!-- Trending Now -->
  <section class="section">
    <div class="section-header">
      <h2 class="section-title">Trending <span class="accent">Now</span></h2>
      <a href="/movies" class="section-link">View All →</a>
    </div>
    <div class="content-grid">
      <% trending.forEach(item => { %>
      <a href="/movies/<%= item.id %>" class="content-card">
        <div class="card-image">
          <img src="<%= item.poster %>" alt="<%= item.title %>" loading="lazy">
          <span class="card-badge <%= item.badge === 'new' ? 'new' : item.premium ? 'premium' : '' %>"><%= item.badge === 'new' ? 'NEW' : item.premium ? '⭐ PREMIUM' : '' %></span>
          <div class="card-overlay">
            <div class="play-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="white"><path d="M8 5v14l11-7z"/></svg>
            </div>
          </div>
        </div>
        <div class="card-info">
          <div class="card-title"><%= item.title %></div>
          <div class="card-meta">
            <span class="rating">★ <%= item.rating %></span>
            <span class="dot"></span>
            <span><%= item.year %></span>
            <span class="dot"></span>
            <span><%= item.genre %></span>
          </div>
        </div>
      </a>
      <% }) %>
    </div>
  </section>

  <!-- Top 10 -->
  <section class="section">
    <div class="section-header">
      <h2 class="section-title">Top 10 in <span class="accent">streamX</span></h2>
    </div>
    <div class="top10-list">
      <% top10.forEach((item, i) => { %>
      <a href="/movies/<%= item.id %>" class="top10-item">
        <span class="top10-number"><%= i + 1 %></span>
        <div class="top10-poster">
          <img src="<%= item.poster %>" alt="<%= item.title %>" loading="lazy">
        </div>
      </a>
      <% }) %>
    </div>
  </section>

  <!-- New Releases -->
  <section class="section">
    <div class="section-header">
      <h2 class="section-title">New <span class="accent">Releases</span></h2>
      <a href="/movies?sort=year" class="section-link">View All →</a>
    </div>
    <div class="content-grid">
      <% newReleases.forEach(item => { %>
      <a href="/movies/<%= item.id %>" class="content-card">
        <div class="card-image">
          <img src="<%= item.poster %>" alt="<%= item.title %>" loading="lazy">
          <span class="card-badge new">NEW</span>
          <div class="card-overlay">
            <div class="play-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="white"><path d="M8 5v14l11-7z"/></svg>
            </div>
          </div>
        </div>
        <div class="card-info">
          <div class="card-title"><%= item.title %></div>
          <div class="card-meta">
            <span class="rating">★ <%= item.rating %></span>
            <span class="dot"></span>
            <span><%= item.genre %></span>
          </div>
        </div>
      </a>
      <% }) %>
    </div>
  </section>

  <!-- Trending Series -->
  <section class="section">
    <div class="section-header">
      <h2 class="section-title">Trending <span class="accent">Series</span></h2>
      <a href="/series" class="section-link">View All →</a>
    </div>
    <div class="content-grid">
      <% trendingSeries.forEach(item => { %>
      <a href="/series/<%= item.id %>" class="content-card">
        <div class="card-image">
          <img src="<%= item.poster %>" alt="<%= item.title %>" loading="lazy">
          <span class="card-badge <%= item.premium ? 'premium' : item.badge === 'new' ? 'new' : '' %>"><%= item.premium ? '⭐ PREMIUM' : item.badge === 'new' ? 'NEW' : item.seasons + ' Seasons' %></span>
          <div class="card-overlay">
            <div class="play-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="white"><path d="M8 5v14l11-7z"/></svg>
            </div>
          </div>
        </div>
        <div class="card-info">
          <div class="card-title"><%= item.title %></div>
          <div class="card-meta">
            <span class="rating">★ <%= item.rating %></span>
            <span class="dot"></span>
            <span><%= item.seasons %> Season<%= item.seasons > 1 ? 's' : '' %></span>
            <span class="dot"></span>
            <span><%= item.genre %></span>
          </div>
        </div>
      </a>
      <% }) %>
    </div>
  </section>

  <!-- Premium CTA -->
  <section class="section">
    <div class="premium-cta">
      <div class="premium-cta-content">
        <h2>Upgrade to <span class="accent">Premium</span></h2>
        <p>Get unlimited access to all movies, series, and exclusive originals in stunning 4K quality.</p>
        <a href="/pricing" class="btn btn-primary btn-lg">View Plans</a>
      </div>
    </div>
  </section>
</main>

<%- include('partials/footer') %>
